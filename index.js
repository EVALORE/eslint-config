import { jsConfig } from './configs/js.js';
import { tsConfig } from './configs/ts.js';

export const configs = {
  js: jsConfig,
  ts: tsConfig,
};

export const rules = {
  js: jsConfig.rules,
  ts: tsConfig.rules,
};
